# 开发任务清单 (Task List)

## 阶段一：基础数据结构与交互框架搭建
- [√] 定义系统核心数据结构（根据需求实现 `athlete`, `item`, `university` 结构体）。
- [√] 梳理系统架构：拆分模块为 **信息输入模块**、**比赛结果录入模块**、**查询模块**。
- [√] 设计并实现基于命令行的系统主菜单（Menu），支持循环选择，提供各个功能模块的入口。
- [ ] 设计文件存储规范，明确各数据在本地文件中的存储与读取格式以便初始化。

## 阶段二：信息录入与报名模块
- [ ] 实现赛事初始化逻辑：提示用户录入参赛学校总数($n$)、男子参赛项目数($m$)、女子参赛项目数($w$)。
- [ ] 实现学校信息录入功能。
- [ ] 实现比赛项目信息录入功能（包括计分规则的选择及 `weight` 数组权值的动态分配）：
  - [ ] 规则1：超过6人取前5名（7,5,3,2,1）
  - [ ] 规则2：不超过6人取前3名（5,3,2）
  - [ ] 规则3：自定义取法指定权值
- [ ] 实现运动员报名登记功能，必须完成以下约束和校验：
  - [ ] 校验限制：每个学校对每个项目的参赛运动员**不能超过 4 人**。
  - [ ] 校验限制：每个运动员**最多只能参加 3 项**单项比赛。
- [ ] 封装文件写入逻辑 `fwrite()`：将录入好的学校、项目及初始化的运动员报名信息持久化存储到本地。

## 阶段三：比赛赛程与成绩录入模块
- [ ] 实现“竞赛检录”功能：每项比赛开始前登记/校验实际参赛人员。
- [ ] 实现“比赛成绩录入”功能：
  - [ ] 程序引导/提醒用户依次录入某个项目前几名获奖运动员的信息。
  - [ ] 根据选定的积分规则及对应权值，自动为获奖运动员关联的学校计算并增加得分。
  - [ ] 实时更新记录对应学校的团体总成绩、男子团体总分、女子团体总分。
- [ ] 封装文件更新逻辑：更改对应项目结构体重指向获奖信息的指针，并更新最终获奖名单至文件系统。

## 阶段四：数据查询与排行统计模块
- [ ] 实现“基础查询”功能：允许查看已录入的参赛学校信息、比赛项目信息。
- [ ] 实现“比赛成绩检索”功能卡片：
  - [ ] 支持按 **竞赛项目** 查询成绩。
  - [ ] 支持按 **参赛学校** 查询成绩。
  - [ ] 支持按 **参赛运动员** 查询单人各项成绩。
- [ ] 实现“排行榜生成排序”功能：能对学校数据结构进行排序算法并逐一打印：
  - [ ] 学校总团体成绩排序榜单
  - [ ] 学校男子总成绩排序榜单
  - [ ] 学校女子总成绩排序榜单

## 阶段五：系统测试与内存优化
- [ ] 检查动态内存管理：确保所有 `new` 配置的内存（包括动态分配的 `weight` 和存放具体项目对象的资源）都有对应的 `delete` 操作，以避免内存泄漏。
- [ ] 完成针对无效菜单选择和越界报名的数据边界异常处理机制。
- [ ] 完善代码注释，打包代码测试可用性。
