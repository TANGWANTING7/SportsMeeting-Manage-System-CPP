# SPORTSMEETING-MANAGE-SYSTEM-CPP 需求文档（同步进度）

## 1. 概述与背景
开发一个校际运动会管理系统文件读写支持（CLI），用于管理多所学校参加的赛事流程。

## 2. 赛事规则与限定
- **项目与参赛方**：设定 $n$ 个参赛学校，$m$ 个男子竞赛项目，$w$ 个女子竞赛项目。每个学校可以选择参加全部项目或只参加部分项目。
- **报名限制**：
  - 每个学校在每个单项的参赛运动员 **不能超过 4 人**。
  - 每个运动员 **最多只能参加 3 项** 单项比赛。
  - 团体赛报名不受限制。

## 3. 积分与计分规则
各项目名次取法及分数权值可以由用户自定义，系统默认采用以下业务规则：
1. **参赛人数超过 6 人**：取前 5 名。积分依次为：第1名得 7 分，第2名得 5 分，第3名得 3 分，第4名得 2 分，第5名得 1 分。
2. **参赛人数不超过 6 人**：取前 3 名。积分依次为：第1名得 5 分，第2名得 3 分，第3名得 2 分。
3. **团体项目**：名次录取规则与上述单项相同，但对应积分加倍。

## 4. 核心功能需求
### 4.1 数据录入与报名模块
- 用户需先输入参赛学校总数、男子参赛项目数、女子参赛项目数。
- **运动员报名登记**：以学校为单位进行。系统需在录入时根据“报名限制”进行数据校验。所有基础信息（学校、项目、运动员）录入后存为独立的数据文件。

### 4.2 赛前检录与比赛模块
- **竞赛检录**：每项比赛正式开始前，针对该项参赛人员完成检录功能。
- **成绩登记录入**：比赛结束后，通过程序弹出提醒让用户填写名次。输入获奖运动员信息，根据检录确认的人数及积分规则，自动生成、登记成绩并增加本校总得分。

### 4.3 查询与统计报表模块
- **基础查询**：随时查看参赛学校信息、比赛项目信息。
- **成绩记录查询**：支持按 `竞赛项目`、`参赛学校` 或 `参赛运动员` 分别查看成绩数据。
- **排行榜排序**：以学校为依据，按总成绩的高低排序，分别输出：各个学校总团体成绩、男子团体总成绩、女子团体总成绩。

### 4.4 交互需求
- 系统提供清晰的 **文本菜单 (Menu)** 工作模式，让用户通过按键选择即可调用上述各类功能。

## 5. 程序数据结构与设计方案 (研发参考)
系统总体设为：信息输入、比赛结果录入、查询三大模块。数据结构采用结构体数组进行内存管理。

```cpp
struct athlete {
    char name[20];   // 姓名
    int age;         // 年龄
    char From[20];   // 来自学校
}; 

struct item {
    char name[20];   // 项目名
    int* weight;     // 动态分配权值空间 (存放第一名、第二名得分等)
    struct athlete* player; // 指向获奖运动员信息的指针
};

struct university {
    char name[20];   // 学校名
    struct item *item; // 竞赛项目指针，根据用户输入动态分配空间
    int score;       // 学校总分
};
```
在实现时，数据结构实例需搭配文件相关操作 (`fopen`, `fwrite`, `fread` 等)，确保业务数据完整存入对应文件中（如 `item.txt`）。
