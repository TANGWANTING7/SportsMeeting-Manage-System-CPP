## data/university.dat

### 文件结构

第一行为 $ numUniversity $ ，表示已注册学校数量。

接下来共有 $ numUniversity $ 组数据，其中每一组的第一行为学校名称。

接下来一行为 $ numName $ ，表示该学校注册的运动员人数。

接下来 $ numName $ 行，每一行第一个为运动员姓名，随后为运动员年龄。

## data/item.dat

### 文件结构

第一行为 $ numMatch $ ，表示已创建项目数量。

接下来共有 $ numMatch $ 组数据，其中每一组的第一行为项目名称。

接下来一行为 $ numPlayer $ ，表示该项目报名的运动员人数。

接下来 $ numPlayer $ 行，每一行第一个为运动员姓名，随后为运动员年龄，最后为运动员所在学校。

## data/score.dat

### 文件结构

第一行为 $ numMatch $ ，表示已创建项目数量。

接下来共有 $ numMatch $ 组数据，其中每一组的第一行为项目名称。

接下来一行为 $ numPlayer $ ，表示该项目报名的运动员人数。

接下来 $ numPlayer $ 行，每一行第一个为运动员姓名，随后为运动员年龄，随后为运动员所在学校，最后为运动员该项目的成绩。